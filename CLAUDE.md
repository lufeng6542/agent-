# 全局指令：并行审查系统

## 核心规则：三阶段并行审查协议

在执行任何非简单任务时，必须遵循以下三阶段协议：

### 阶段一：方案评审（提出工作方案时触发）

当主程序制定工作方案后、开始执行前，**必须**启动 3 个并行 Agent 进行方案评审：

```
并行 Agent 1 — 完整性审查：
  检查方案是否遗漏了关键步骤、边界条件或依赖项
  评估方案的覆盖范围是否足够全面

并行 Agent 2 — 风险评估：
  识别方案中可能的失败点、潜在错误和风险
  检查是否存在安全性问题或破坏性操作
  评估每一步的回滚难度

并行 Agent 3 — 优化建议：
  寻找更高效或更简洁的替代方案
  检查是否有过度工程化或不必要的复杂度
  评估方案是否符合最小化原则
```

**执行方式**：使用 Agent 工具同时启动 3 个 subagent_type=general-purpose 的 Agent（单个消息中包含 3 个 Agent 工具调用）。收集所有反馈后，向用户展示整合后的评审结果，确认后再执行。

**豁免条件**（不需要评审的任务）：
- 单行代码修改或 typo 修复
- 纯信息查询（读文件、搜索等）
- 用户明确要求跳过评审

### 阶段二：并行资料查询（需要多源信息时触发）

当需要从不同来源获取信息时，**应**启动并行 Agent 分别查询：

```
判断条件：需要查询的信息来源 ≥ 2 个不同位置/文件/目录/网页

执行方式：
  - 每个信息源启动一个独立的 Agent（subagent_type=Explore 用于文件搜索，general-purpose 用于复杂查询）
  - 所有 Agent 在单个消息中并行启动
  - 收集所有结果后整合，再继续主任务
```

### 阶段三：完成检查（任务完成后触发）

主程序完成任务后，**必须**启动 3 个并行 Agent 进行完成度检查：

```
并行 Agent 1 — 需求符合度检查：
  对照用户原始需求，逐条验证是否满足
  检查是否有遗漏的用户要求
  验证输出是否符合用户期望的格式和质量

并行 Agent 2 — 代码/文档质量检查：
  检查代码质量：命名规范、结构清晰度、潜在 bug
  检查文档质量：格式规范、内容准确、逻辑连贯
  验证是否符合项目既有的编码/写作规范

并行 Agent 3 — 方案建议回顾：
  回顾阶段一和阶段二中提出的所有建议
  检查哪些建议已被采纳，哪些被忽略
  评估被忽略的建议是否需要补充处理
```

**执行方式**：使用 Agent 工具同时启动 3 个 Agent。将检查结果呈现给用户，如有问题则进行修正。

**豁免条件**（不需要完成检查的任务）：
- 纯信息查询的返回结果
- 用户明确要求跳过检查
- 对话上下文中已有明确的完成确认

## 实施要点

1. **并行 Agent 的 prompt 必须包含完整上下文**：每个 Agent 都需要知道当前任务的背景、方案内容和用户需求，因此 prompt 要足够详细
2. **评审结果要整合后再呈现**：不要直接列出 3 个 Agent 的原始输出，而是整合为结构化的评审报告
3. **用户确认优先**：阶段一评审后必须等待用户确认才执行；阶段三检查后如有问题需修正
4. **保持简洁**：并行审查不应显著增加输出长度，评审报告要精炼
